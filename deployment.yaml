apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
        - name: react-app
          image: app  # This should match your loaded Kind image
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
